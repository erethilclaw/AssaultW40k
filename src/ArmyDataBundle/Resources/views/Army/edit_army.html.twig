{% extends "base.html.twig" %}
{% block title %}{{ 'army.edt'|trans }}{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function () {
            $('#army').addClass('active');
        });
    </script>
{% endblock %}
    {% block page_content %}
        {% form_theme edit_form 'ArmyDataBundle:Error:form_errors.html.twig' %}
        <!-- Page Content -->
        <div class="col-md-6 col-lg-6 col-sm-6 col-lg-offset-3 col-md-offset-3 col-sm-offset-3">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">{{ 'army.edt'|trans }}</h3><br>
                    <a href="{{ path('army_index') }}">{{ 'generic.list'|trans }}</a>
                </div>
                {{ form_start(edit_form,{'attr': {'role': 'form'}}) }}
                {{ form_errors(edit_form) }}
                <div class="box-body">
                    <div class="form-group ">
                        {{ form_label(edit_form.name) }}
                        <div style="color: red">
                            {{ form_errors(edit_form.name) }}
                        </div>
                        {{ form_widget(edit_form.name, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="col-md-4">
                        <div class="thumbnail">
                            <img style="max-width:128px" src={{ asset('uploads/army/' ~ army.image.filename) }}>
                            <div class="caption">
                                <p class="text-center">{{ 'generic.current_img'|trans }}</p>
                            </div>
                        </div>
                    </div>
                    <div class="form-group ">
                        {% if edit_form.image|length > 0 %}<img src"{{ edit_form.image }}">{% endif %}
                        {{ form_row(edit_form.image) }}
                    </div>
                </div>
                <div class="box-footer">
                    <button class="btn btn-primary" type="submit">{{ 'army.edt'|trans }}</button>
                </div>
                {{ form_end(edit_form) }}
            </div>
        </div>
        <!-- /.container -->
    {% endblock %}
{% block leng %}
    {% set locale = app.request.locale %}
    <li class="Button">
        <a href="{{ path(app.request.attributes.get('_route'), { _locale: 'en', 'id':army.id }) }}">
            {% if locale == 'en' %}
                <img src="{{ asset('dist/img/eng_flag.png') }}"/>
            {% else %}
                <img src="{{ asset('dist/img/eng_bw_flag.png') }}">
            {% endif %}
        </a>
    </li>
    <li>
        <a href="{{ path(app.request.attributes.get('_route'), { _locale: 'es', 'id':army.id  }) }}">
            {% if locale == 'es' %}
                <img src="{{ asset('dist/img/es_flag.png') }}">
            {% else %}
                <img src="{{ asset('dist/img/es_bw_flag.png') }}">
            {% endif %}
        </a>
    </li>
{% endblock %}
